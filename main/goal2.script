
function init(self)
	
end 
function on_message(self, message_id, message, sender)
	if message_id == hash("trigger_response") then
		if  message.other_group == hash("ball") then
			goal2()
			print("goal")
		end
	end
end

function goal2()
	msg.post("/gui", "goal2")
end